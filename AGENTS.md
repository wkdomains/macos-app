# rules

run xcodebuild ONLY if the change was complex, for most changes skip this.
Don't do git status, don't do git diff.
no need to write a summary
just change the code and reply "----------> DONE"
